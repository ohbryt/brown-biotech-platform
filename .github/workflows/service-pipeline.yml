name: Brown Biotech Service Pipeline

on:
  push:
    branches:
      - service
  workflow_dispatch:

concurrency:
  group: brown-biotech-service-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "20"
  NEXT_PUBLIC_SITE_URL: https://services.brownbio.tech
  SERVICE_DOMAIN: services.brownbio.tech

jobs:
  build-and-deploy:
    name: Build and deploy service site
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Prepare Vercel project link
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_SERVICE }}
        run: |
          mkdir -p .vercel
          cat > .vercel/project.json <<EOF
          {"orgId":"$VERCEL_ORG_ID","projectId":"$VERCEL_PROJECT_ID"}
          EOF

      - name: Deploy to Vercel
        id: deploy
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          vercel build --prod --yes --token "$VERCEL_TOKEN"
          DEPLOY_JSON=$(vercel deploy --prod --yes --token "$VERCEL_TOKEN" --json)
          echo "$DEPLOY_JSON"
          DEPLOY_URL=$(python3 -c 'import json,sys; print(json.loads(sys.stdin.read())["url"])' <<< "$DEPLOY_JSON")
          echo "deployment_url=$DEPLOY_URL" >> "$GITHUB_OUTPUT"

      - name: Alias service domain
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          DEPLOY_URL: ${{ steps.deploy.outputs.deployment_url }}
        run: |
          vercel alias set "$DEPLOY_URL" "$SERVICE_DOMAIN" --token "$VERCEL_TOKEN"