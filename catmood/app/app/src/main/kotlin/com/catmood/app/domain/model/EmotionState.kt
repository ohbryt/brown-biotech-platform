package com.catmood.app.domain.model

/** Shared contract — taxonomy v1 (root AGENTS.md). Do not extend without doc update. */
enum class EmotionState {
    RELAXED_CONTENT,
    ALERT_AROUSED,
    FEARFUL_ANXIOUS,
    IRRITATED_AGGRESSIVE,
    PAIN_DISCOMFORT,
}
