package com.catmood.app.domain.model

/** Shared contract — CatMeows-derived contexts. UNCERTAIN is the model's reject option. */
enum class MeowContext {
    FOOD_REQUEST,
    ATTENTION_ISOLATION,
    HANDLING_CONTACT,
    UNCERTAIN,
}
