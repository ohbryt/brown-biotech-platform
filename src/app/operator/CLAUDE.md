# Operator Portal

## What This Does
Internal operator dashboard — not customer-facing. Operator-facing tools and monitoring.

## Key Files
- `page.tsx` — Main operator page
- `layout.tsx` — Sidebar layout

## Commands
```bash
cd src/app/operator && npm run dev
```